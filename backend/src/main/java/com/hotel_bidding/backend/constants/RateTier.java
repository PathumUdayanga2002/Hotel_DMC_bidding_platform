package com.hotel_bidding.backend.constants;

/**
 * Rate tiers for hotel pricing
 */
public enum RateTier {
    WISH,    // Highest rate
    WANT,    // Mid-tier rate (requires approval)
    WALK     // Lowest rate (final offer)
}
